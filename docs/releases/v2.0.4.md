# GTD Space {{tag}}

## Highlights

- Fixed a startup sidebar bug that showed a red error toast for `Purpose and Principles` on workspaces that correctly use the canonical `Purpose & Principles` folder.
- Kept section alias compatibility without treating missing legacy alias folders as real load failures.

## Notable Changes

- Sidebar section preloading and refresh now resolve a single real horizon directory before loading files, instead of eagerly loading every alias candidate.
- Added a regression test that covers the `Purpose & Principles` / `Purpose and Principles` alias case so the startup error does not come back.
- No workspace migration or data format changes are required for this patch.

## Installation

Download the installer or package that matches your platform:

- Windows: `.msi` installer or `.exe` (NSIS) installer
- macOS Intel: `.dmg` for Intel Macs
- macOS Apple Silicon: `.dmg` for Apple Silicon Macs
- Linux: `.AppImage` (universal) or `.deb` (Debian/Ubuntu)

## Notes

- `v2.0.4` is the recommended patch release on top of the `v2.0.x` line.
- This patch is focused on removing a noisy startup error and preserving expected horizon-folder compatibility.
